import { UniqueProjectName } from './unique-project-name';

describe('UniqueProjectName', () => {
  it('should create an instance', () => {
    expect(new UniqueProjectName()).toBeTruthy();
  });
});
